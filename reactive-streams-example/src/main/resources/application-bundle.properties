# CONSTANT PROPERTIES
log.content=Message received from Kafka : ${body} on the topic ${headers[kafka.TOPIC]} on the partition ${headers[kafka.PARTITION]} with the offset ${headers[kafka.OFFSET]} with the key ${headers[kafka.KEY]}
kafka.brokers=localhost:9092
kafka.component.uri=kafka:fuse-demo?brokers={{kafka.brokers}}
dynamic.reactive.component=reactive-streams:file-content

# PROPERTIES SHOULD BE CHANGED AS PER REQUIREMENT
file.output.location=/home/kodtodya/Downloads/_test
output.file-name=kafka-rx-stream-content-file.txt
file.component.uri=file:{{file.output.location}}?fileName={{output.file-name}}&fileExist=Append
